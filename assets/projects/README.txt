Replace these three placeholder files with your real Altium CAD renders,
keeping the same file names (or update the src="" and data-full="" paths
in index.html if you rename them):

  solar-cad-1.jpg   <- shown as the main image by default
  solar-cad-2.jpg
  solar-cad-3.jpg

Tips:
- Export renders around 1200-1600px wide — no need for anything larger,
  it'll just slow the page down.
- JPG is fine for solid renders; use PNG if a render has transparency.
- Want more or fewer than 3 images? Copy or delete a
  <button class="gallery-thumb">...</button> block in index.html
  for each one, matching the pattern already there.
